<div class="main">
  <div class="inner">
    <div class="user__wrapper">
      <h2 class="page-heading">
        <%= "#{@user.name}さんの情報"%>  プロフィールを編集する
      </h2>
      <table class="table">
        <tbody>
          <tr>
            <th class="table__col1">名前</th>
            <td class="table__col2"><%= @user.name %></td>
            <% unless user_signed_in? && current_user.id %>
              <td class="table__col3"><%="メッセージを送る" %></td>
              <td class="table__col4"><%="フレンドリクエストを送る" %></td>
            <% end %>
          </tr>
          <%# <tr>
            <th class="table__col1">プロフィール</th>
            <td class="table__col2"><%= @user.profile %></td>
          <%# </tr>
          <tr>
            <th class="table__col1">所属</th>
            <td class="table__col2"><%= @user.occupation %></td>
          <%# </tr>
          <tr>
            <th class="table__col1">役職</th>
            <td class="table__col2"><%= @user.position %></td>
          <%# </tr>  %>
        </tbody>
      </table>
      <h2 class="page-heading">
        <%= "#{@user.name}さんの投稿" %>
      </h2>
      <div class="user__card">
        <%= render partial: "tweets/tweet", collection: @tweets %>
      </div>
    </div>
  </div>
</div>