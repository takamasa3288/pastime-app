<div class="card">
  <%= link_to image_tag(tweet.image.variant(resize: '500x500'), class: :card__img), tweet_path(tweet.id) if tweet.image.attached? %>
  <div class="card__body">
    <%= tweet.content %>
    <%# <% <%= link_to "by #{tweet.user.name}", user_path(tweet.user), class: :card__user %> 
  </div>
</div>